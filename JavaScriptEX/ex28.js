var num1 = 10;
  var num2 = 20;
  function sum(x, y) {
    var result = x + y;
    return result;
  }

  var mutlply = function(x, y) {
    var result = x * y;
    return result;
  }

  var sumResult = sum(num1, num2);
  var multiplyResult = mutlply(num1, num2);